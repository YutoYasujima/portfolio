<div id="follows" class="user-cards"
>
  <%# 一覧表示 %>
  <% followings.each do |following| %>
    <%= render "shared/user_card", user: following, option: render("users/follows/follow_buttons", user: following), buttons: nil %>
  <% end %>
</div>